# Backend Environment Variables
# Copy this file to .env and update with your actual values

# Server Configuration
NODE_ENV=development
PORT=8080

# CORS Configuration
# For local development (frontend on port 5173)
CORS_ORIGIN=http://localhost:5173
# For Docker (frontend on port 5174), use: CORS_ORIGIN=http://localhost:5174

# MongoDB Configuration
MONGO_URL=mongodb://localhost:27017/aio
# For Docker, use: MONGO_URL=mongodb://mongodb:27017/aio

# JWT Configuration
# IMPORTANT: Generate a strong random secret (at least 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_min_32_chars_required
JWT_EXPIRES_IN=7d

# Cookie Configuration
COOKIE_NAME=aio_session
COOKIE_DOMAIN=localhost

# CSRF Configuration (optional - defaults to JWT_SECRET if not set)
# CSRF_SECRET=your_csrf_secret_here
# CSRF_COOKIE_NAME=csrf_secret

# Request Size Limits (optional)
# MAX_JSON_SIZE=10mb
# MAX_URLENCODED_SIZE=1mb

# GitHub OAuth (optional - for future integration)
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# Cursor API (optional - for future integration)
# CURSOR_API_KEY=your_cursor_api_key

# Webhook Configuration (optional - for future integration)
# GITHUB_WEBHOOK_SECRET=your_github_webhook_secret
# PLATFORM_WEBHOOK_TOKEN=your_platform_webhook_token

